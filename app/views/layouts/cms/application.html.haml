!!!
%html
  %head
    %title CMS
    %link{ :href => 'https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css', :rel => 'stylesheet' }/
    %link{ :href => '//oss.maxcdn.com/clusterize.js/0.16.0/clusterize.css', :rel => 'stylesheet' }/
    %script{ :src => '//oss.maxcdn.com/clusterize.js/0.16.0/clusterize.min.js' }
    %script{ :src => '//cdn.tinymce.com/4/tinymce.min.js' }
    %meta{ :content => 'width=device-width, initial-scale=1, shrink-to-fit=no', :name => 'viewport' }/
    %meta{ :content => 'ie=edge', 'http-equiv' => 'x-ua-compatible' }/
    = stylesheet_link_tag 'cms/application', media: 'all'
    = javascript_include_tag 'cms/application'
    = csrf_meta_tags
  %body
    #collapsing-nav.collapse.p-y-1
      - Cms::Configuration.resources.each do |resource|
        = link_to resource.route_name.titleize, polymorphic_path(resource.route_name), class: 'btn btn-link btn-block'
    .navbar.navbar-full.navbar-dark.bg-primary.m-b-3
      .hidden-md-up
        %button.navbar-toggler{ 'data-target' => '#collapsing-nav', 'data-toggle' => :collapse, :type => :button }
          \&#9776;
      .container.hidden-sm-down
        %ul.nav.navbar-nav
          - Cms::Configuration.resources.each do |resource|
            %li.nav-item{ :class => ('active' if current_page?(File.join('/cms/', resource.route_path))) }
              = link_to resource.route_name.titleize, polymorphic_path(resource.route_name), class: 'nav-link'
    .container.p-b-3
      - if flash[:alert] || notice.present?
        .p-b-2
          - if flash[:alert]
            - flash[:alert].each do |alert|
              = render 'cms/shared/alert', message: alert, type: 'danger'
          - if notice.present?
            = render 'cms/shared/alert', message: notice, type: 'success'
      = yield
